# CICC Mac Excel 插件验收清单

本清单对应严格镜像 `技能_CICC图表+底稿_260803.md` 的版本。实机测试只能操作工作簿副本，禁止修改原始 `造纸周度数据库_260125.xlsx`。

## 2026-08-05 自动化结果

- [x] `npm test`：17 个测试文件、177/177 测试通过，无 skipped tests。
- [x] `npm run typecheck` 通过。
- [x] `npm run build` 通过，`dist/` 含 commands、feedback、taskpane 三个 HTML 入口及 assets。
- [x] `npm run manifest:dev` 通过，开发 manifest 已生成。
- [x] `git diff --check` 通过。
- [ ] `npm run manifest:validate`：当前沙箱无法解析 `validationgateway.omex.office.net`，必须在联网环境重跑，不能记为 schema 通过。
- [ ] 可信 HTTPS 页面检查：需在可读取 Office 开发证书的会话中启动服务器后检查 `/taskpane.html` 与 `/commands.html`。

## 固定回归：Mac 普通折线图

在 `/private/tmp/CICC_Addin_Smoke_260125.xlsx` 的 `Sheet1!A1:C13` 使用以下数据：

- A1 空白，B1=`A`，C1=`B`。
- A2:A13 为 2001-01-01 至 2012-01-01。
- B2:B13 为 1 至 12，C2:C13 恒为 5。

验收项：

- [x] 单元测试证明默认自动识别为按列，分类地址为 A2:A13，系列 A/B 地址分别为 B2:B13、C2:C13。
- [x] Office.js 边界测试证明折线与散点系列不会调用 `ChartSeriesFormat.fill.setSolidColor`。
- [ ] Mac Excel 点击“中金工具 → 普通折线图”后，原生图表稳定保留，不被回滚删除。
- [ ] 图表含 A/B 两个完整系列名及 2001–2012 日期分类。
- [ ] 保存、关闭、重开副本后图表仍存在。
- [ ] Excel 日志中没有新的折线系列 `ChartFill.setSolidColor` 负 HResult。

## 数据映射与可用性

- [x] 自动方向：仅顶部有系列文本标签时按列；仅左侧有系列文本标签时按行；两边都有时按较长数据方向；方阵平局按列。
- [x] 明确选择“按列”或“按行”时覆盖自动判断。
- [x] 日期、多系列、文本分类、百分比、负数、空值及八系列配色循环均有回归测试。
- [x] 散点图的文本 X 或非数值 Y 在写入前被拒绝。
- [x] 饼图只接受一个数值系列，多系列在写入前被拒绝。
- [x] 图表失败时删除本次新图表，并保留 `create`、`series-style`、`common-style`、`final-sync` 阶段诊断。
- [x] 高级窗格只保留图表类型和自动/按列/按行，不再提供会破坏固定样式的视觉覆盖项。

## 十种原生图表实机验收

- [ ] 普通柱形图。
- [ ] 堆积柱形图。
- [ ] 普通折线图。
- [ ] 带标记折线图。
- [ ] 饼图。
- [ ] 条形图。
- [ ] 散点图＋线性趋势线。
- [ ] 分离饼图。
- [ ] 百分比堆积柱形图。
- [ ] 堆积折线图。

每一种兼容图表都必须同时满足：

- [ ] 固定 16×9 cm，放在选区右侧；临近工作表最右边界时改放选区下方。
- [ ] 无图表标题、轴标题、数据标签、来源文字、网格线和外边框。
- [ ] 图例固定下方、非覆盖、Arial 9pt、黑色。
- [ ] 坐标轴显式可见，刻度向外，标签位于轴旁，轴文字 Arial 8pt、黑色。
- [ ] 折线全部平滑、1.5pt；普通折线无标记，带标记折线显示标记。
- [ ] 柱/条系列有填充且无描边；饼图按数据点着色；散点与折线不访问系列填充。
- [ ] 系列色依次为 `#640000`、`#8A2626`、`#3D889A`、`#646C86`、`#BE995D`、`#DD965D`，超过六个后循环。
- [ ] 系列名称完整保留括号内的币种和单位。

## 表格与斑马纹

- [x] 自动化证明“格式化表格”和“斑马纹”为两个独立计划。
- [ ] 单独执行“格式化表格”：表头 `#8A2626`、白字、加粗、8pt；数据区白底、黑字、8pt。
- [ ] 格式化表格后全部左对齐、垂直居中、自动换行、行高 16、无任何边框，工作表网格线关闭。
- [ ] “格式化表格”不自动增加斑马纹。
- [ ] 单独执行“斑马纹”：第一行数据 `#F5F5F5`，下一行白色，随后交替；不改表头。
- [x] 自动化证明斑马纹只写入数据行填充，不改字体、边框、对齐或内容。
- [ ] 先格式化再加斑马纹后，红色表头保持不变。
- [ ] 两种操作前后 values、formulas、number formats、merged cells、conditional formats 均保持一致。

## 安装、持久化与隐私

- [ ] Excel for Mac 16.111.2 旁加载 `manifest.dev.xml` 后显示“中金工具”页签和四个入口：生成图表、高级生成、格式化表格、斑马纹。
- [ ] 高级任务窗格可打开、生成图表、显示成功/错误提示并正常关闭。
- [ ] 完全退出并重启 Excel 后，页签、命令和已保存图表仍可用。
- [ ] 只向静态 host 和 Office.js CDN 请求资源，不上传选区、values、formulas 或图表数据。
- [x] 原始工作簿只读校验：2026-08-05 SHA-256 为 `8e5a128649d33278ba87b0bbec4e90cbb8c61298e3e3a789c45361f60cbb5e01`，大小 13,055,090 bytes，修改时间 2026-01-25 17:13:58 +0800。

## 当前实机阻塞

2026-08-05 本轮尝试通过 Computer Use 控制 Microsoft Excel 时，桌面控制权限未获系统批准；同时自动审批额度已耗尽，无法通过终端启动需读取用户 Office 证书的 HTTPS 服务。因此本文件只勾选已有直接证据的自动化项，所有 Mac Excel 实机项保持未勾选。
